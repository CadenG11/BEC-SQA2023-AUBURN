name: Run My Script

on: [push]

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@main

    - name: Run Python script
      run: python fuzz.py